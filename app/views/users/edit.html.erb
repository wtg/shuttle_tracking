<% content_for :title, "Editing #{@user.username}" %>
<div class="main-title">Editing <%= @user.username %></div>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>


