<% content_for :title, "Editing #{@vehicle.name}" %>
<div class="main-title">Editing <%= @vehicle.name %></div>

<%= render 'form' %>

<%= link_to 'Show', @vehicle %> |
<%= link_to 'Back', vehicles_path %>
